# ShipWatch API

Base URL: `http://localhost:8080`

## Endpoints

- `GET /health`
- `GET /endpoints`
- `POST /checks/run`
- `GET /checks`
- `GET /checks/latest`
- `GET /incidents`

## Configuration

Use either `CONFIG_FILE=config/endpoints.json` or `TARGETS="GitHub=https://github.com"`.
